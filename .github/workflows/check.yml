name: "Nix Flake Check"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixos-unstable
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Check flake
        run: nix flake check --impure --all-systems

      - name: Build nas1 configuration
        run: nix build .#nixosConfigurations.nas1.config.system.build.toplevel --impure --no-link

      - name: Build nas2 configuration
        run: nix build .#nixosConfigurations.nas2.config.system.build.toplevel --impure --no-link
